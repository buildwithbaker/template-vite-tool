import './styles/main.css';

const app = document.querySelector('#app');

app.innerHTML = `
  <section class="card">
    <h1>Build with Baker — tool template</h1>
    <p>
      Replace this with your tool. Edit
      <code>src/main.js</code> and <code>src/styles/main.css</code>,
      then update <code>README.md</code> and the page <code>&lt;title&gt;</code>.
    </p>
  </section>
`;
