{
  "name": "template-vite-tool",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Build with Baker — starter template for a small Vite tool (v2.0 repo standard).",
  "author": "Adam Baker / Build with Baker",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.0"
  }
}
